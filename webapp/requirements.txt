# Rebar Detection Web Application - Requirements
# Python 3.10+
#
# NOTE: PyTorch is NOT listed here because pip installs CPU-only by default.
#       Run setup.sh to auto-detect your GPU and install the correct version.
#       Or install manually:
#         GPU:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#         CPU:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.19
jinja2==3.1.4
aiofiles==24.1.0

# Machine Learning (PyTorch installed separately via setup.sh)
numpy>=1.24.0
pillow>=10.0.0
opencv-python-headless>=4.8.0

# Utilities
pycocotools>=2.0.7
requests>=2.31.0
huggingface_hub>=0.20.0
